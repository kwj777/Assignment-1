# Library Management System API

A FastAPI backend for tracking library books and the members who borrow them, built for SDEV 3310.

Work for each assignment lives on its own branch so the repository shows cumulative progress.

| Branch | Content |
|---|---|
| `assignment-1` | API foundation, data models, and validation |

To view an assignment locally, run:

```bash
git switch assignment-1
```
