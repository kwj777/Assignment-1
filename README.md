# Library Management System API

> ## ➡️ The Assignment 1 code is on the [**`assignment-1`**](../../tree/assignment-1) branch.
>
> This `main` branch is deliberately kept as the repository starting point, so each
> assignment branch shows cumulative progress. The application itself lives on the
> assignment branch.
>
> To get it directly:
>
> ```bash
> git clone -b assignment-1 https://github.com/kwj777/Assignment-1.git
> ```

A FastAPI backend for tracking library books and the members who borrow them, built for SDEV 3310.

## Branches

| Branch | Content |
|---|---|
| `main` | Repository starting point (this file) |
| [`assignment-1`](../../tree/assignment-1) | API foundation, data models, and validation |

To view an assignment in an existing clone:

```bash
git switch assignment-1
```
